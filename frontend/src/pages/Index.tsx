import Card from '../components/Card';
import '../css/Main.css';

export default () => {
	return (<Card>
		<h2 style={{marginTop: 0}}>Plan your day like never before.</h2>
		<p style={{marginBottom: 0}}>With TodoManager, you can manage tasks,<br />
		and track the amount of work you've done.</p>
	</Card>);
};